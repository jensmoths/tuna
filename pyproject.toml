[project]
name = "tuna-tune"
version = "0.1.0"
description = "Tuna durable state and helper tooling"
requires-python = ">=3.10"
dependencies = ["Flask>=3.0"]

[project.scripts]
tune = "tune.cli.main:main"
